========================================
QUANTA CLI COMMANDS CHEAT SHEET
========================================

1. BUILDING
-----------
cargo build --release


2. RUNNING NODE
---------------
# Start node
./target/release/quanta start

# Start with custom config
./target/release/quanta start -c quanta.toml

# Start in detached mode (background)
./target/release/quanta start --detach


3. WALLET OPERATIONS
--------------------
# Create new wallet
./target/release/quanta new-wallet --file wallet.qua

# View wallet details
./target/release/quanta wallet --file wallet.qua

# Get balance
curl -X POST http://localhost:3000/api/balance -H "Content-Type: application/json" -d '{"address":"YOUR_ADDRESS"}'


4. MINING
---------
# Start mining (RPC)
./target/release/quanta start_mining <WALLET_ADDRESS> --rpc-port 7782

# Stop mining (RPC)
./target/release/quanta stop_mining --rpc-port 7782

# Mine single block (API)
curl -X POST http://localhost:3000/api/mine -H "Content-Type: application/json" -d '{"miner_address":"YOUR_ADDRESS"}'


5. API ENDPOINTS
----------------
# Get stats
curl http://localhost:3000/api/stats

# Get peer count
curl http://localhost:3000/api/peers

# Get mempool info
curl http://localhost:3000/api/mempool


6. MAINTENANCE
--------------
# Clean all data (Linux/WSL)
rm -rf ~/quanta_data logs wallet.qua

# Check logs
tail -f logs/quanta.log

cargo run -q --bin get_testnet_hash


REAL_TESTNET_GENESIS_HASH: 4dbf3c4375b6fb25bc01a45c19ace8d656374ceb7e712ab3d88a70f29793034a


Time Gen
powershell -Command "[DateTimeOffset]::Parse('2026-03-21T00:00:00Z').ToUnixTimeSeconds()"

//docker COMMANDS

docker build -t xd637/quanta-node:alpha-v2 -t xd637/quanta-node:latest .


docker push xd637/quanta-node:alpha-v2

docker push xd637/quanta-node:latest


/// server setup - 

sudo apt update && sudo apt upgrade -y

curl -fsSL https://get.docker.com | sh

sudo usermod -aG docker ubuntu && newgrp docker

//ip ports - 

sudo iptables -I INPUT -p tcp --dport 8333 -j ACCEPT
sudo iptables -I INPUT -p tcp --dport 7782 -j ACCEPT
sudo iptables -I INPUT -p tcp --dport 3000 -j ACCEPT
sudo apt install -y iptables-persistent
sudo netfilter-persistent save

run node -

mkdir -p ~/quanta_data

sudo chmod 777 ~/quanta_data

docker run -d \
  --name quanta-node \
  --restart always \
  -p 8333:8333 \
  -p 7782:7782 \
  -p 3000:3000 \
  -v ~/quanta_data:/home/quanta/quanta_data \
  xd637/quanta-node:latest

//check logs - docker logs quanta-node --tail 30 -f

//stop and clean start

docker stop quanta-node && docker rm quanta-node
docker pull xd637/quanta-node:latest
docker run -d \
  --name quanta-node \
  --restart always \
  -p 8333:8333 \
  -p 7782:7782 \
  -p 3000:3000 \
  -v ~/quanta_data:/home/quanta/quanta_data \
  xd637/quanta-node:latest

docker build -t xd637/quanta-node:v0.7.0-alpha -t xd637/quanta-node:latest .

docker push xd637/quanta-node:v0.7.0-alpha
docker push xd637/quanta-node:latest

